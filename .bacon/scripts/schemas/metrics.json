{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "timestamp": {"type": "string", "format": "date-time"},
      "event": {"type": "string"},
      "status": {"type": "string", "enum": ["success", "failed", "rejected", "approved"]}
    },
    "required": ["timestamp", "event", "status"]
  }
}
