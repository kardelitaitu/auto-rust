# Plan

## What Is the Solution
1. **Flatten Validation Paths**: Refactor `validate_task_with_known_tasks` using early returns (`?` operator) and extracting nested validation blocks into dedicated helper functions.
2. **Test Extraction**: Move the 1,000+ lines of validation integration tests to `tests/task_validation_tests.rs`.

# internal api outline

Implementation details to be defined during active development.

# decisions

Implementation details to be defined during active development.

