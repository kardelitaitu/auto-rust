version: 1
id: 0032-configurable-stagger-delay
title: Expose task staggering delay as TASK_STAGGER_DELAY_MS environment override
status: approved
owner: spec-agent
implementer: pending
priority: P3
area:
  - configuration
  - concurrency
files:
  code:
    - src/config/env.rs
  docs:
    - docs/specs/_active/0032-configurable-stagger-delay/spec.yaml
    - docs/specs/_active/0032-configurable-stagger-delay/plan.md
    - docs/specs/_active/0032-configurable-stagger-delay/validation.md
acceptance:
  - "TASK_STAGGER_DELAY_MS environment variable override is parsed as a u64 and overrides config.orchestrator.task_stagger_delay_ms."
  - "Unit tests verify that task_stagger_delay_ms is correctly loaded from environment overrides."
  - "All unit, integration, and formatting checks pass successfully."
non_goals:
  - "Do not alter the staggering mechanism itself."
risks:
  - "Invalid values (e.g., negative or non-integer) must be gracefully ignored, preserving the default delay."
