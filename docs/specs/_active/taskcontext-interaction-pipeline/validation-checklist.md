# Validation Checklist

- [ ] Click and type share one shared internal interaction pipeline.
- [ ] Public TaskContext verb names and signatures stay unchanged.
- [ ] Click, nativeclick, focus, select_all, clear, keyboard, and type_text still work on representative selectors.
- [ ] Post-action pause is applied consistently across the shared flow.
- [ ] Verification behavior is explicit and testable, not hidden in duplicated method code.
- [ ] `click_and_wait` still behaves as a composition of click plus wait.
- [ ] `.
check-fast.ps1` passes during implementation.
- [ ] `.
check.ps1` passes before handoff.
