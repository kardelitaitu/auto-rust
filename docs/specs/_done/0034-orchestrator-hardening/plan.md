## Plan

1. Reconfirm the current orchestrator behavior and the existing test surface.
2. Add the smallest regression tests that cover cancellation, timeout, and guard drop behavior.
3. Verify the package with `spec-lint.ps1`, then implementation checks after approval.
