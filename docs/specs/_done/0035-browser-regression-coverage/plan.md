## Plan

1. Confirm the current filter and port tests already cover the stable baseline.
2. Add only the missing deterministic regressions for discovery ordering and empty inputs.
3. Validate the package and keep browser discovery mocks local and lightweight.
