## Validation

- Property tests cover `predict_engagement` finite bounded outputs.
- Property tests cover feature extraction and combination on arbitrary inputs.
- An explicit empty-string regression is present.
- The existing fixed-case scorer tests still pass.
- `spec-lint.ps1` passed before handoff.
