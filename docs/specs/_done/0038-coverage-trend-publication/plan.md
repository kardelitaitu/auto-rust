## Plan

1. Confirm the CI coverage job can emit a stable JSON summary without changing the gate.
2. Add the retained publication step for that summary.
3. Keep the local `coverage.ps1` path aligned with the CI output.
4. Verify the workflow text and repo checks stay green.
