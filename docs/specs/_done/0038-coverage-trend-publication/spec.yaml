version: 1
id: 0038-coverage-trend-publication
title: Coverage trend publication
status: done
owner: spec-agent
implementer: archived-pending
priority: P2
area:
  - ci
  - coverage
  - docs
files:
  code:
    - .github/workflows/ci.yml
  docs:
    - docs/specs/_done/0038-coverage-trend-publication/README.md
    - docs/specs/_done/0038-coverage-trend-publication/plan.md
    - docs/specs/_done/0038-coverage-trend-publication/validation.md
    - docs/specs/_done/0038-coverage-trend-publication/notes.md
acceptance:
  - The CI coverage job publishes a machine-readable summary at a stable path, such as `target/reports/coverage/coverage.json`.
  - The published coverage summary is retained in a form that can be compared across runs.
  - Local `coverage.ps1` keeps producing the same HTML and JSON outputs.
  - `./check.ps1` passes.
non_goals:
  - Build a coverage dashboard.
  - Change the 40% gate.
  - Change runtime behavior or test semantics.
risks:
  - Artifact retention may expire before a later comparison job uses it.
  - A future schema change could break downstream consumers of the JSON summary.
  - The CI workflow may grow by one or two steps.
