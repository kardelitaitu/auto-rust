## Plan

1. Confirm the current CI nextest invocation and repo config baseline.
2. Add the smallest safe CI profile settings for failure output, retries, and timeout behavior.
3. Keep the local `check.ps1` path unchanged.
4. Verify the repo gate still passes and the CI workflow remains aligned.
