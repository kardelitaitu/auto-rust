version: 1
id: 0039-nextest-config-hardening
title: Nextest config hardening
status: done
owner: spec-agent
implementer: archived-pending
priority: P2
area:
  - testing
  - ci
  - performance
files:
  code:
    - .config/nextest.toml
    - .github/workflows/ci.yml
  docs:
    - docs/specs/_done/0039-nextest-config-hardening/README.md
    - docs/specs/_done/0039-nextest-config-hardening/plan.md
    - docs/specs/_done/0039-nextest-config-hardening/validation.md
    - docs/specs/_done/0039-nextest-config-hardening/notes.md
acceptance:
  - The CI nextest profile explicitly sets failure output and retry behavior.
  - The CI nextest profile defines timeout behavior suitable for catching hung tests.
  - The repo still passes `./check.ps1` after the config change.
  - The CI workflow continues to use `cargo nextest run --all-features --profile ci`.
non_goals:
  - Add new tests.
  - Change production runtime behavior.
  - Add a full performance dashboard.
risks:
  - A too-aggressive timeout could interrupt legitimate long-running tests.
  - Retry settings could hide flaky behavior if used too broadly.
  - CI logs may become noisier with more explicit failure output.
